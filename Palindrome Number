class Solution:
    def isPalindrome(self, x: int) -> bool:
        rev = 0
        num = x
        while num > 0:
            remainder = num % 10
            rev = 10 * rev + remainder
            num = num // 10
        
        #print(x)
        #print(rev)
        return (x == rev)
